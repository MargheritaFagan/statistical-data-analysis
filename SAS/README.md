# SAS

Academic group project on customer purchasing behaviour.

The code includes dataset sorting and merging, creation of derived variables, customer-level aggregation, descriptive statistics, frequency tables and statistical tests.
